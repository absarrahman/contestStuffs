#include <bits/stdc++.h>
#define ll long long
using namespace std;

struct StudentData{
  string name;
  int id;
  int marks;
};

bool cmpStd()

int main(){
    int n;
    scanf("%d",&n); //takes number of students
    StudentData s[n];
    for(auto i: s){
        scanf("%d",&s[i].id);
        cin>>s[i].name;
        scanf("%d",&s[i].marks);
    }
    sort()
    return 0;
}

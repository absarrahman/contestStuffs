#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int testCase;
    cin>>testCase;
    while(testCase--){
        int n,d;
        cin>>n>>d;
        if((d<=n)||((d-n)==1)){
            cout<<"YES"<<"\n";
        } else {
            for(int i= 1;i<=n-1;i++){

            }
        }
    }
    return 0;
}
